{
  "name": "drone-safe",
  "version": "1.0.0",
  "description": "Is it safe to fly my drone today?",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node dist/tests/rating.test.js"
  }
}
